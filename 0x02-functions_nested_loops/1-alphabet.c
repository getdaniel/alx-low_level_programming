#include "main.h"

/**
 * main - Entry point of the program
 *
 * Return: Always 0 for success
 */
int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(i);

	_putchar('\n');

	return (0);
}
